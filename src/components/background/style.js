import styled, {css} from 'styled-components';
import {FlexCol, Img} from '../globals';

const ScrewWrapper = styled.div`
  width: 6%;
  height: 6%;
`

const TopLeftDiv = ScrewWrapper.extend`
  position: absolute;
  left: 2.6%;
  top: 1.1%;
`;
